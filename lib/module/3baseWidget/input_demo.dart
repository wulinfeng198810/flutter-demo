import 'package:flutter/material.dart';

class BaseWidget_InputDemo extends StatelessWidget {
  const BaseWidget_InputDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
