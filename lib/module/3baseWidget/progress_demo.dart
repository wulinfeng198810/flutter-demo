import 'package:flutter/material.dart';

class BaseWidget_ProgressDemo extends StatelessWidget {
  const BaseWidget_ProgressDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
