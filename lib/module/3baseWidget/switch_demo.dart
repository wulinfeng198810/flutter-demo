import 'package:flutter/material.dart';

class BaseWidget_SwitchDemo extends StatelessWidget {
  const BaseWidget_SwitchDemo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
